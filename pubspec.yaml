name: awid
description: AWID - Transport de colis France Algerie
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.9.1
  url_launcher: ^6.3.1
  intl: ^0.20.2
  image_picker: ^1.1.2
  file_picker: ^8.1.7
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
